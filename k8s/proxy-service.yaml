apiVersion: v1
kind: Service
metadata:
  name: todo-proxy
spec:
  type: NodePort
  selector:
    app: todo-proxy
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
