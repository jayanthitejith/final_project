apiVersion: v1
kind: Service
metadata:
  name: todo-proxy
spec:
  type: NodePort
  selector:
    app: todo-proxy
  ports:
    - protocol: TCP
      port: {{ .Values.proxy.port }}
      targetPort: {{ .Values.proxy.port }}
      nodePort: {{ .Values.proxy.nodePort }}
